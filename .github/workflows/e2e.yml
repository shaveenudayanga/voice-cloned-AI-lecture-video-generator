name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    # E2E requires running infrastructure — Phase 7 will flesh this out
    steps:
      - uses: actions/checkout@v4
      - name: E2E placeholder
        run: echo "E2E tests added in Phase 7 (Playwright against full stack)"
